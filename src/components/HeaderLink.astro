---
export interface Props {
  to: string;
}

const { to } = Astro.props;
const isActive = to === Astro.url.pathname;
---

<a href={to} class:list={["text-white", isActive && "underline"]}>
  <slot />
</a>
