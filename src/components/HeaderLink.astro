---
export interface Props {
  to: string;
}

const { to } = Astro.props;
const isActive = to === Astro.url.pathname;
---

<a href={to} class={isActive ? "underline text-black dark:text-white" : null}>
  <slot />
</a>
