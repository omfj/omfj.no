---
export interface Props {
  title: string;
  subtitle: string;
}

const { title, subtitle } = Astro.props;
---

<div class="flex flex-col mb-5">
  <h1 class="text-3xl font-bold text-center mb-3">{title}</h1>
  <p class="text-center text-gray-300">
    {subtitle}
  </p>
</div>
